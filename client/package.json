{
  "name": "server-status-client",
  "version": "0.3.00",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:netlify": "vite build",
    "predeploy": "echo Use 'npm run deploy' from project root",
    "deploy": "npm -C .. run deploy"
  }
}
